const CONTROLLER = `import { ResponseBody } from '@am92/express-utils'
import {rsrcPrefix}Model from '{modelPath}'

const {rsrcPrefix}Controller = {}

export default {rsrcPrefix}Controller`

export default CONTROLLER
