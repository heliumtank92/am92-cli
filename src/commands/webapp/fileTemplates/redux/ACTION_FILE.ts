const ACTION_FILE = `import { createAction } from '@reduxjs/toolkit'
import traceActionsCreator from '../traceActionsCreator'

// export const sampleAction = createAction('{reducerName}/sample')`

export default ACTION_FILE
